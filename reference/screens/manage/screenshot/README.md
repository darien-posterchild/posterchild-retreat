# Manage Overview — Visual Reference Screenshot

Place the exported PNG reference here as:

```text
reference/screens/manage/screenshot/manage-reference.png
```

### Export Guidelines:
1. **Source Frame**: Select the Manage Overview frame (`Node 512:6840`) in Figma.
2. **Format**: Export as `1x PNG` (or `2x PNG` for Retina comparison).
3. **Usage**: This screenshot will be used for pixel-level visual QA and automated/manual visual regression checks against the live React rendering.
